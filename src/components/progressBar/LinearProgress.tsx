import { LinearProgress } from "@mui/material";

export default function LinearProgressBar() {
    return (
        <LinearProgress
            sx={{ width: "100%", my: 1, height: 5, backgroundColor: "#e0e0e0" }}
        />
    );
}